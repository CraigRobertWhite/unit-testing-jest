/**
 * Adds two numbers
 * @param {number} augend - The augend to add to
 * @param {number} addend - The addend to add by
 * @returns {number} The sum of the two numbers
 */
function add(augend, addend) {
    return augend + addend;
}

module.exports = add;
